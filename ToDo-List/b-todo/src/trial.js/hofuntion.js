const numbers = [-2,-1,0,1,2]
const itemms = numbers.map(n => ({number:n}))
const itemms1 = numbers.filter(n => n>=0)
const itemms2 = numbers.filter(n => n>=0).map(n => ({number:n}))

console.log(itemss)
console.log(itemss1)
console.log(itemss2)
